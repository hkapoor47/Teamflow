import DashboardLayout from "../layouts/DashboardLayout.jsx";
import { useProjects } from "../context/ProjectContext.jsx";
function Notifications() { const { claimRequests, tasks, members } = useProjects(); return <DashboardLayout><div className="teamflow-page"><section className="teamflow-heading"><p className="welcome-label">NOTIFICATIONS</p><h2>Manager updates.</h2><p className="welcome-description">Task requests and delivery updates, ready for backend email integration later.</p></section><section className="panel notification-list">{claimRequests.length ? claimRequests.map((request) => { const task = tasks.find((item) => item.id === request.taskId); const member = members.find((item) => item.id === request.memberId); return <div className="notification-item" key={request.id}><span>✦</span><div><strong>{member?.name} requested a task</strong><p>They want to claim “{task?.title}”. Status: {request.status}.</p></div></div>; }) : <div className="empty-state"><span>✓</span><strong>No new notifications</strong><p>Task claim requests will appear here.</p></div>}</section></div></DashboardLayout>; }
export default Notifications;
